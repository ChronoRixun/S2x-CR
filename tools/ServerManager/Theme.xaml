<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- The launcher's tokens (tools/ServerLauncher.xaml), same values. -->
  <SolidColorBrush x:Key="Accent"     Color="#FFE8A33D"/>
  <SolidColorBrush x:Key="AccentHot"  Color="#FFF4BB66"/>
  <SolidColorBrush x:Key="Ok"         Color="#FF5FBF7A"/>
  <SolidColorBrush x:Key="Danger"     Color="#FFC7524A"/>
  <SolidColorBrush x:Key="Ink"        Color="#FFE6E8EA"/>
  <SolidColorBrush x:Key="Label"      Color="#FFA8B0B6"/>
  <SolidColorBrush x:Key="Muted"      Color="#FF8A9299"/>
  <SolidColorBrush x:Key="Dim"        Color="#FF6F777D"/>
  <SolidColorBrush x:Key="Faint"      Color="#FF5C646B"/>
  <SolidColorBrush x:Key="Ghost"      Color="#FF3A4147"/>
  <SolidColorBrush x:Key="StatusOff"  Color="#FF4A5157"/>
  <SolidColorBrush x:Key="Line"       Color="#FF1C2024"/>
  <SolidColorBrush x:Key="Edge"       Color="#FF262B30"/>
  <SolidColorBrush x:Key="EdgeHot"    Color="#FF3A4147"/>
  <SolidColorBrush x:Key="BtnEdge"    Color="#FF2E343A"/>
  <SolidColorBrush x:Key="IconEdge"   Color="#FF23282D"/>
  <SolidColorBrush x:Key="Field"      Color="#FF16191C"/>
  <SolidColorBrush x:Key="Row"        Color="#FF121417"/>
  <SolidColorBrush x:Key="Panel"      Color="#FF0E1012"/>
  <SolidColorBrush x:Key="Bar"        Color="#FF0A0B0D"/>
  <SolidColorBrush x:Key="Knob"       Color="#FF0C0D0F"/>
  <SolidColorBrush x:Key="TagBg"      Color="#FF1A1710"/>
  <SolidColorBrush x:Key="TagEdge"    Color="#FF3A2F1C"/>
  <SolidColorBrush x:Key="DangerBg"   Color="#FF1C1211"/>
  <SolidColorBrush x:Key="DangerEdge" Color="#FF4A2A28"/>
  <SolidColorBrush x:Key="Divider"    Color="#FF24282C"/>
  <SolidColorBrush x:Key="WindowEdge" Color="#FF2A2F34"/>

  <!-- The desk behind the cards: a 24 px rule grid. -->
  <DrawingBrush x:Key="Desk" TileMode="Tile" Viewport="0,0,24,24" ViewportUnits="Absolute" Stretch="None">
    <DrawingBrush.Drawing>
      <DrawingGroup>
        <GeometryDrawing Brush="#FF0E1012">
          <GeometryDrawing.Geometry><RectangleGeometry Rect="0,0,24,24"/></GeometryDrawing.Geometry>
        </GeometryDrawing>
        <GeometryDrawing Brush="#FF111417">
          <GeometryDrawing.Geometry>
            <GeometryGroup>
              <RectangleGeometry Rect="23,0,1,24"/>
              <RectangleGeometry Rect="0,23,24,1"/>
            </GeometryGroup>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>
      </DrawingGroup>
    </DrawingBrush.Drawing>
  </DrawingBrush>

  <!-- No map art exists, so the plate carries a hatch instead. -->
  <DrawingBrush x:Key="Hatch" TileMode="Tile" Viewport="0,0,12,12" ViewportUnits="Absolute" Stretch="None">
    <DrawingBrush.Transform><RotateTransform Angle="45"/></DrawingBrush.Transform>
    <DrawingBrush.Drawing>
      <DrawingGroup>
        <GeometryDrawing Brush="#FF0E1012">
          <GeometryDrawing.Geometry><RectangleGeometry Rect="0,0,12,12"/></GeometryDrawing.Geometry>
        </GeometryDrawing>
        <GeometryDrawing Brush="#FF121417">
          <GeometryDrawing.Geometry><RectangleGeometry Rect="0,0,6,12"/></GeometryDrawing.Geometry>
        </GeometryDrawing>
      </DrawingGroup>
    </DrawingBrush.Drawing>
  </DrawingBrush>

  <!-- ── text ─────────────────────────────────────────────────────────────── -->
  <Style x:Key="Mono" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="Consolas"/>
    <Setter Property="FontSize" Value="11"/>
    <Setter Property="Foreground" Value="{StaticResource Faint}"/>
  </Style>
  <Style x:Key="Caps" TargetType="TextBlock">
    <Setter Property="FontSize" Value="10"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="Foreground" Value="{StaticResource Dim}"/>
  </Style>
  <Style x:Key="Stat" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="Consolas"/>
    <Setter Property="FontSize" Value="26"/>
    <Setter Property="Foreground" Value="{StaticResource Ink}"/>
  </Style>

  <!-- The state dot. A server that is up but has not answered yet pulses. -->
  <Style x:Key="StateDot" TargetType="Rectangle">
    <Setter Property="Width" Value="7"/>
    <Setter Property="Height" Value="7"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
    <Style.Triggers>
      <DataTrigger Binding="{Binding IsStarting}" Value="True">
        <DataTrigger.EnterActions>
          <BeginStoryboard x:Name="PulseDot">
            <Storyboard>
              <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0.3"
                               Duration="0:0:0.5" AutoReverse="True" RepeatBehavior="Forever"/>
            </Storyboard>
          </BeginStoryboard>
        </DataTrigger.EnterActions>
        <DataTrigger.ExitActions>
          <StopStoryboard BeginStoryboardName="PulseDot"/>
        </DataTrigger.ExitActions>
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <!-- ── buttons ──────────────────────────────────────────────────────────── -->
  <!-- One template; Tag carries the hover background so the variants stay short. -->
  <Style x:Key="Flat" TargetType="Button">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Foreground" Value="{StaticResource Ink}"/>
    <Setter Property="BorderBrush" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="12,0"/>
    <Setter Property="FontSize" Value="11"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Tag" Value="{StaticResource Row}"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="HorizontalContentAlignment" Value="Center"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="bd" Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              Margin="{TemplateBinding Padding}"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="bd" Property="Background"
                      Value="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.4"/>
              <Setter Property="Cursor" Value="Arrow"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="Caption" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Width" Value="40"/>
    <Setter Property="Height" Value="40"/>
    <Setter Property="Foreground" Value="{StaticResource Muted}"/>
    <Setter Property="FontWeight" Value="Normal"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Tag" Value="{StaticResource Field}"/>
  </Style>
  <Style x:Key="CaptionClose" TargetType="Button" BasedOn="{StaticResource Caption}">
    <Setter Property="Tag" Value="{StaticResource Danger}"/>
  </Style>

  <Style x:Key="Primary" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Background" Value="{StaticResource Accent}"/>
    <Setter Property="Foreground" Value="{StaticResource Knob}"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="FontSize" Value="11.5"/>
    <Setter Property="Padding" Value="16,9"/>
    <Setter Property="Tag" Value="{StaticResource AccentHot}"/>
  </Style>

  <Style x:Key="DangerGhost" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Foreground" Value="{StaticResource Muted}"/>
    <Setter Property="BorderBrush" Value="{StaticResource DangerEdge}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="FontSize" Value="11.5"/>
    <Setter Property="Padding" Value="14,8"/>
    <Setter Property="Tag" Value="{StaticResource DangerBg}"/>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Foreground" Value="{StaticResource Danger}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Danger}"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="Outline" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="BorderBrush" Value="{StaticResource BtnEdge}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="FontWeight" Value="Normal"/>
    <Setter Property="Padding" Value="14,8"/>
    <Setter Property="Tag" Value="Transparent"/>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Foreground" Value="{StaticResource Accent}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Accent}"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- CARDS | ROSTER: the background and foreground come from the view model. -->
  <Style x:Key="Segment" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Padding" Value="12,0"/>
    <Setter Property="Tag" Value="{StaticResource TagBg}"/>
  </Style>

  <!-- Card footer buttons -->
  <Style x:Key="CardAction" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Foreground" Value="{StaticResource Label}"/>
    <Setter Property="Padding" Value="8,0"/>
    <Setter Property="Tag" Value="{StaticResource Row}"/>
    <Setter Property="ToolTipService.ShowOnDisabled" Value="True"/>
    <Style.Triggers>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="IsMouseOver" Value="True"/>
          <Condition Property="IsEnabled" Value="True"/>
        </MultiTrigger.Conditions>
        <Setter Property="Foreground" Value="{StaticResource Accent}"/>
      </MultiTrigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="CardStart" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Background" Value="{StaticResource Accent}"/>
    <Setter Property="Foreground" Value="{StaticResource Knob}"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="Tag" Value="{StaticResource AccentHot}"/>
  </Style>

  <Style x:Key="CardStop" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Foreground" Value="{StaticResource Muted}"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="Tag" Value="{StaticResource DangerBg}"/>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Foreground" Value="{StaticResource Danger}"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="CardRestart" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Background" Value="{StaticResource TagBg}"/>
    <Setter Property="Foreground" Value="{StaticResource Accent}"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="Tag" Value="{StaticResource Accent}"/>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Foreground" Value="{StaticResource Knob}"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="CardCopy" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Foreground" Value="{StaticResource Dim}"/>
    <Setter Property="FontFamily" Value="Consolas"/>
    <Setter Property="FontSize" Value="10.5"/>
    <Setter Property="FontWeight" Value="Normal"/>
    <Setter Property="Padding" Value="8,0"/>
    <Setter Property="Tag" Value="{StaticResource Row}"/>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Foreground" Value="{StaticResource Ink}"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="Starter" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Background" Value="{StaticResource Bar}"/>
    <Setter Property="BorderBrush" Value="{StaticResource Line}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="0"/>
    <Setter Property="Tag" Value="{StaticResource Row}"/>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="BorderBrush" Value="{StaticResource TagEdge}"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="RosterRow" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Padding" Value="0"/>
    <Setter Property="Tag" Value="{StaticResource Row}"/>
    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="bd" Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}">
            <ContentPresenter/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="bd" Property="Background" Value="{StaticResource Row}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ── editor fields ────────────────────────────────────────────────────── -->
  <Style x:Key="Entry" TargetType="TextBox">
    <Setter Property="Background" Value="{StaticResource Field}"/>
    <Setter Property="Foreground" Value="{StaticResource Ink}"/>
    <Setter Property="BorderBrush" Value="{StaticResource Edge}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="8,5"/>
    <Setter Property="CaretBrush" Value="{StaticResource Accent}"/>
    <Setter Property="SelectionBrush" Value="{StaticResource Accent}"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Style.Triggers>
      <Trigger Property="IsKeyboardFocusWithin" Value="True">
        <Setter Property="BorderBrush" Value="{StaticResource Accent}"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="Number" TargetType="TextBox" BasedOn="{StaticResource Entry}">
    <Setter Property="FontFamily" Value="Consolas"/>
    <Setter Property="FontSize" Value="12.5"/>
    <Setter Property="TextAlignment" Value="Right"/>
    <Setter Property="Padding" Value="6,4"/>
  </Style>

  <!-- The mockup's little track and knob, for shuffle, halftime and advertise. -->
  <Style x:Key="Switch" TargetType="ToggleButton">
    <Setter Property="Width" Value="30"/>
    <Setter Property="Height" Value="16"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Focusable" Value="False"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Border x:Name="track" Background="{StaticResource Field}" BorderBrush="{StaticResource Edge}"
                  BorderThickness="1" SnapsToDevicePixels="True">
            <Rectangle x:Name="knob" Width="10" Height="10" Fill="{StaticResource Ink}"
                       HorizontalAlignment="Left" Margin="2,0,0,0"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="track" Property="Background" Value="{StaticResource Accent}"/>
              <Setter TargetName="knob" Property="HorizontalAlignment" Value="Right"/>
              <Setter TargetName="knob" Property="Margin" Value="0,0,2,0"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Map and mode pickers. The stock ComboBox brings its own light chrome. -->
  <Style x:Key="PickerItem" TargetType="ComboBoxItem">
    <Setter Property="Foreground" Value="{StaticResource Label}"/>
    <Setter Property="Padding" Value="9,6"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBoxItem">
          <Border x:Name="bd" Background="Transparent" Padding="{TemplateBinding Padding}">
            <ContentPresenter/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
              <Setter TargetName="bd" Property="Background" Value="{StaticResource Row}"/>
              <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter Property="Foreground" Value="{StaticResource Accent}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="Picker" TargetType="ComboBox">
    <Setter Property="Height" Value="36"/>
    <Setter Property="Foreground" Value="{StaticResource Ink}"/>
    <Setter Property="ItemContainerStyle" Value="{StaticResource PickerItem}"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBox">
          <Grid>
            <ToggleButton x:Name="toggle" Focusable="False" ClickMode="Press"
                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
              <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                  <Border x:Name="face" Background="{StaticResource Field}" BorderBrush="{StaticResource Edge}"
                          BorderThickness="1" SnapsToDevicePixels="True">
                    <Path Data="M0,0 L8,0 L4,5 Z" Fill="{StaticResource Muted}"
                          HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                  </Border>
                  <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                      <Setter TargetName="face" Property="BorderBrush" Value="{StaticResource EdgeHot}"/>
                    </Trigger>
                  </ControlTemplate.Triggers>
                </ControlTemplate>
              </ToggleButton.Template>
            </ToggleButton>
            <ContentPresenter Margin="9,0,26,0" VerticalAlignment="Center" IsHitTestVisible="False"
                              Content="{TemplateBinding SelectionBoxItem}"
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"/>
            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" Focusable="False"
                   AllowsTransparency="True" PopupAnimation="None">
              <Border Background="{StaticResource Bar}" BorderBrush="{StaticResource Edge}" BorderThickness="1"
                      MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="320">
                <ScrollViewer VerticalScrollBarVisibility="Auto"><ItemsPresenter/></ScrollViewer>
              </Border>
            </Popup>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Bot fill. The pips above it carry the value; this is only the handle. -->
  <Style x:Key="FillSlider" TargetType="Slider">
    <Setter Property="Height" Value="16"/>
    <Setter Property="IsMoveToPointEnabled" Value="True"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Slider">
          <Grid Background="Transparent">
            <Rectangle Height="2" Fill="{StaticResource Edge}" VerticalAlignment="Center"/>
            <Track x:Name="PART_Track">
              <Track.Thumb>
                <Thumb Width="9" Height="16">
                  <Thumb.Template>
                    <ControlTemplate TargetType="Thumb">
                      <Rectangle Fill="{StaticResource Accent}"/>
                    </ControlTemplate>
                  </Thumb.Template>
                </Thumb>
              </Track.Thumb>
              <Track.IncreaseRepeatButton>
                <RepeatButton Command="Slider.IncreaseLarge" Opacity="0" Focusable="False"/>
              </Track.IncreaseRepeatButton>
              <Track.DecreaseRepeatButton>
                <RepeatButton Command="Slider.DecreaseLarge" Opacity="0" Focusable="False"/>
              </Track.DecreaseRepeatButton>
            </Track>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Rotation rows: the list is the mockup's, selection is a bar down the left. -->
  <Style x:Key="RotationItem" TargetType="ListBoxItem">
    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
    <Setter Property="Padding" Value="0"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListBoxItem">
          <Border x:Name="marker" BorderBrush="Transparent" BorderThickness="2,0,0,0" Margin="0,0,0,2">
            <ContentPresenter/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="marker" Property="BorderBrush" Value="{StaticResource Accent}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="RotationList" TargetType="ListBox">
    <Setter Property="Background" Value="Transparent"/>
    <!-- A ListBox brings the system's own text colour, which is black on this panel. -->
    <Setter Property="Foreground" Value="{StaticResource Ink}"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="0"/>
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
    <Setter Property="ItemContainerStyle" Value="{StaticResource RotationItem}"/>
  </Style>

  <!-- The small square buttons at the end of a rotation row. -->
  <Style x:Key="RowIcon" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Width" Value="24"/>
    <Setter Property="Height" Value="24"/>
    <Setter Property="Padding" Value="0"/>
    <Setter Property="FontWeight" Value="Normal"/>
    <Setter Property="Foreground" Value="{StaticResource Dim}"/>
    <Setter Property="BorderBrush" Value="{StaticResource IconEdge}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Margin" Value="3,0,0,0"/>
    <Setter Property="Tag" Value="Transparent"/>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Foreground" Value="{StaticResource Ink}"/>
        <Setter Property="BorderBrush" Value="{StaticResource EdgeHot}"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="RowRemove" TargetType="Button" BasedOn="{StaticResource RowIcon}">
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Foreground" Value="{StaticResource Danger}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Danger}"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="Swatch" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Width" Value="18"/>
    <Setter Property="Height" Value="18"/>
    <Setter Property="Padding" Value="0"/>
    <Setter Property="Margin" Value="0,0,4,0"/>
    <Setter Property="BorderThickness" Value="1"/>
  </Style>

  <Style x:Key="Link" TargetType="Button" BasedOn="{StaticResource Flat}">
    <Setter Property="Foreground" Value="{StaticResource Dim}"/>
    <Setter Property="FontWeight" Value="Normal"/>
    <Setter Property="FontSize" Value="11.5"/>
    <Setter Property="Padding" Value="2,4"/>
    <Setter Property="Tag" Value="Transparent"/>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Foreground" Value="{StaticResource Danger}"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- ── scrollbar ────────────────────────────────────────────────────────── -->
  <Style TargetType="ScrollBar">
    <Setter Property="Width" Value="8"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ScrollBar">
          <Grid Background="Transparent">
            <Track x:Name="PART_Track" IsDirectionReversed="True">
              <Track.Thumb>
                <Thumb>
                  <Thumb.Template>
                    <ControlTemplate TargetType="Thumb">
                      <Rectangle Fill="#FF262B30"/>
                    </ControlTemplate>
                  </Thumb.Template>
                </Thumb>
              </Track.Thumb>
              <Track.IncreaseRepeatButton>
                <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
              </Track.IncreaseRepeatButton>
              <Track.DecreaseRepeatButton>
                <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
              </Track.DecreaseRepeatButton>
            </Track>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
